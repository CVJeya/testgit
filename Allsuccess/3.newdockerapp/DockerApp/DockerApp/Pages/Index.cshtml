@page
@model IndexModel
@{
    ViewData["Title"] = "Data from Backend Web API";
}

<div class="text-center">
    <h1 class="display-4">Fetched Data from Backend</h1>
   
</div>

Docker Application and a Docker API are now connected using an ASP.NET APPLICATION using docker-compose
<div style="color: #D8000C; background-color: #FFBABA">
  @Model.ErrorMessage
</div> 

<table class="table">
   <thead>
      <tr>
         <th>Pizza Name</th>
         <th>Ingredients</th>
         <th>Cost</th>
         <th>In Stock</th>
      </tr>
   </thead>
   <tbody>
      @foreach (var pizza in @Model.Pizzas)
      {
         <tr>
            <td>@pizza.PizzaName</td>
            <td>@pizza.Ingredients</td>
            <td>$@pizza.Cost</td>
            <td>@pizza.InStock</td>
         </tr>
      }
   </tbody>
</table>

